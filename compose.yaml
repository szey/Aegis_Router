services:
  aegis-router:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - aegis-router-audit:/app/data
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

volumes:
  aegis-router-audit:
